// Browser dialog:

// display a message and returns undefined
alert("A message")

// asks for confirmation and returns a boolean
confirm("Do you want to proceed?")

// asks for input
// returns a string of the input value if OK is selected
// returns null if cancelled
prompt("What is your name?")

const name = prompt("What is your name?")
const a = 3
const b = 4
const result = a * b 

console.log(`Hello, ${name}! In case you forgot, ${a} x ${b} is ${result}`)